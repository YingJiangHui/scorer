import {useWindowResize} from "@/app/shared/hooks/useWindowResize";
import {useCallback, useMemo} from "react";

export const useDynamicSVGLetterSpace = ()=>{
    useWindowResize(useCallback(()=>{

    },[]))
}